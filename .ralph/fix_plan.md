# Fix Plan

## TODO
- [x] ParquetTypeMapping: Created `ParquetTypeMapping.java` with all 5 type mappings (NUMERIC→REQUIRED INT64, SORTED_NUMERIC→REPEATED INT64, BINARY→REQUIRED BINARY, SORTED→REQUIRED BINARY, SORTED_SET→REPEATED BINARY) plus messageType/columnDescriptor/level helpers. `ParquetTypeMappingTests` with 11 test methods × 10 iterations all passing.
- [x] ParquetDocValuesWriter: Created `ParquetDocValuesWriter` extending `DocValuesConsumer`. Implements all 5 `addXxxField` methods using `ColumnWriteStoreV1` with custom `CapturingPageWriteStore`/`CapturingPageWriter` for in-memory page capture. Writes encoded pages to `.pdvd` data file with field metadata (offsets, types, page counts) in `.pdvm` metadata file. Uses Lucene `CodecUtil` headers/footers. `ParquetDocValuesWriterTests` with 9 test methods × 10 iterations all passing.
- [x] ParquetDocValuesReader: Created `ParquetDocValuesReader` extending `DocValuesProducer`. Reads `.pdvd`/`.pdvm` metadata and data files, reconstructs Parquet pages via `PageReadStore`/`PageReader`, decodes values using `ColumnReadStoreImpl`/`ColumnReader`. Returns `NumericDocValues`, `SortedNumericDocValues`, `BinaryDocValues`, `SortedDocValues`, `SortedSetDocValues` iterators. Handles dictionary encoding for SORTED/SORTED_SET, repetition/definition levels for repeated types. `ParquetDocValuesReaderTests` with 9 round-trip test methods × 10 iterations all passing.
- [x] ParquetDocValuesFormat and conformance tests: Created `ParquetDocValuesFormat` extending `DocValuesFormat` with `.pdvd`/`.pdvm` extensions. Created `ParquetDocValuesFormatTests` extending `BaseDocValuesFormatTestCase`. ALL 131 conformance tests pass with `-Dtests.iters=3`. Fixed multiple bugs during conformance: (1) SPI registration via `META-INF/services/org.apache.lucene.codecs.DocValuesFormat`, (2) sparse doc values support — writer stores doc ID arrays, reader uses binary search for O(log n) lookups, (3) `advanceExact()` contract — must set `doc=target` even when returning false so `docID()` is correct, (4) binary buffer reuse — use `fromReusedByteArray` in writer and clone bytes in reader, (5) `DocValuesSkipper` implementation — single-interval skipper computed from in-memory data, (6) idempotent `close()` in writer.
- [x] Wire into PerFieldMappingPostingFormatCodec: Added `parquetDvFormat` field. Modified `getDocValuesFormatForField()` to return `ParquetDocValuesFormat` when `mapperService.getIndexSettings().isParquetDocValuesEnabled()`. Also fixed pre-existing bug: `IndexSettings` constructor was reading `INDEX_DOC_VALUES_FORMAT_SETTING` via `scopedSettings.get()` which throws when the feature flag is disabled (setting not registered). Fixed by reading from raw `settings` with default value, following the established `IS_WARM_INDEX_SETTING` pattern for feature-flagged settings. All codec tests pass with `-Dtests.iters=3`.
- [x] Disable _source storage: In `SourceFieldMapper.preParse()`, added `if (context.indexSettings().isParquetDocValuesEnabled()) { return; }` after the `isDerivedSourceEnabled()` check. All existing SourceFieldMapperTests pass with `-Dtests.iters=3`. Note: cannot add a unit test in SourceFieldMapperTests because the parquet feature flag is not in BUILT_IN_FEATURE_FLAGS, so the index setting cannot be registered in OpenSearchSingleNodeTestCase. Full verification deferred to integration test.
- [x] _source reconstruction from parquet: In `IndexShard`, changed the `isDerivedSourceEnabled()` condition to `isDerivedSourceEnabled() || isParquetDocValuesEnabled()` so the `DerivedSourceDirectoryReader.wrap()` is also applied for parquet indices. All DerivedSource unit tests and IndexShardTests pass with `-Dtests.iters=1`.
- [x] Integration test: Created `ParquetDocValuesIT` in `server/src/internalClusterTest/java/org/opensearch/index/codec/parquet/` extending `OpenSearchIntegTestCase`. Added `FeatureFlags.PARQUET_DOC_VALUES_SETTING` to `FeatureFlagSettings.BUILT_IN_FEATURE_FLAGS` so the feature flag is recognized by test cluster nodes. 5 tests: `testSourceReconstruction` (verifies _source rebuilt from parquet columns), `testAggregations` (terms, sum, date_histogram on parquet doc_values), `testSorting` (numeric sort via parquet NUMERIC), `testUpdateApi` (partial update via reconstructed _source), `testForceMerge` (full write→read→merge→write cycle). All 5 tests pass with `-Dtests.iters=3`.
- [ ] Parquet export REST API: Create `RestParquetExportAction`, `ParquetExportAction`, `ParquetExportRequest`, `ParquetExportResponse`, `TransportParquetExportAction`. Endpoint: `POST /my-index/_parquet_export?path=<path>`. Reads doc_values via `ParquetDocValuesReader`, writes standalone `.parquet` files with valid footers. Write integration test. Run `./gradlew :server:internalClusterTest --tests "*ParquetExport*" -Dtests.iters=3`.
- [ ] Benchmark with opensearch-benchmark http_logs: Build with `./gradlew localDistro`. Run baseline (default settings) and parquet config. Compare: (1) correctness 100% pass (2) ingestion throughput 2x improvement (3) query performance within 10% (4) storage size 70% reduction.

## Completed
- [x] Feature flag and index setting: Added `opensearch.experimental.feature.parquet_doc_values.enabled` feature flag in `FeatureFlags.java`, `index.codec.doc_values.format` setting in `IndexSettings.java` with `isParquetDocValuesEnabled()` accessor, registered in `IndexScopedSettings.java`.
- [x] Parquet library dependency: Added `parquet-column`, `parquet-encoding`, `parquet-common` 1.15.1 to `server/build.gradle` via version catalog. Created LICENSE/NOTICE files under `server/licenses/`. Added thirdPartyAudit exclusions for parquet shaded classes. All precommit checks pass.
- [x] Lucene I/O adapters: Created `LuceneOutputFile` (adapts `IndexOutput` to Parquet `PositionOutputStream`) and `LuceneInputFile` (adapts `IndexInput` to Parquet `SeekableInputStream`) in `server/src/main/java/org/opensearch/index/codec/parquet/`. Tests in `LuceneIOAdapterTests` with 12 test methods × 10 iterations all passing.

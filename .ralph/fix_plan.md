# Fix Plan

## TODO
- [x] ParquetTypeMapping: Created `ParquetTypeMapping.java` with all 5 type mappings (NUMERIC→REQUIRED INT64, SORTED_NUMERIC→REPEATED INT64, BINARY→REQUIRED BINARY, SORTED→REQUIRED BINARY, SORTED_SET→REPEATED BINARY) plus messageType/columnDescriptor/level helpers. `ParquetTypeMappingTests` with 11 test methods × 10 iterations all passing.
- [ ] ParquetDocValuesWriter: Create `ParquetDocValuesWriter` extending `DocValuesConsumer`. Implement `addNumericField`, `addBinaryField`, `addSortedField`, `addSortedNumericField`, `addSortedSetField` using parquet-column encoders. Write to `.pdvd`/`.pdvm` files via `LuceneOutputFile`. Follow `Composite912DocValuesWriter` pattern for file management. Write `ParquetDocValuesWriterTests`. Run with `-Dtests.iters=10`.
- [ ] ParquetDocValuesReader: Create `ParquetDocValuesReader` extending `DocValuesProducer`. Read `.pdvd`/`.pdvm` via `LuceneInputFile`. Return `NumericDocValues`, `SortedNumericDocValues`, `BinaryDocValues`, `SortedDocValues`, `SortedSetDocValues` iterators. Write `ParquetDocValuesReaderTests` with round-trip tests for all 5 types. Run with `-Dtests.iters=10`.
- [ ] ParquetDocValuesFormat and conformance tests: Create `ParquetDocValuesFormat` extending `DocValuesFormat` with extensions `.pdvd`/`.pdvm`. Create `ParquetDocValuesFormatTests` extending Lucene's `BaseDocValuesFormatTestCase`. ALL conformance tests must pass. Run with `-Dtests.iters=3`.
- [ ] Wire into PerFieldMappingPostingFormatCodec: Add `parquetDvFormat` field. Modify `getDocValuesFormatForField()` to return `ParquetDocValuesFormat` when `mapperService.getIndexSettings().isParquetDocValuesEnabled()`. Run `./gradlew :server:test --tests "org.opensearch.index.codec.*" -Dtests.iters=3`.
- [ ] Disable _source storage: In `SourceFieldMapper.preParse()`, add `if (context.indexSettings().isParquetDocValuesEnabled()) { return; }` after the `isDerivedSourceEnabled()` check. Run `./gradlew :server:test --tests "*.SourceFieldMapperTests" -Dtests.iters=3`.
- [ ] _source reconstruction from parquet: In `IndexShard`, where `DerivedSourceDirectoryReader.wrap()` is called for `isDerivedSourceEnabled()`, add same wrapping for `isParquetDocValuesEnabled()`. Run `./gradlew :server:internalClusterTest --tests "*.DerivedSource*" -Dtests.iters=1`.
- [ ] Integration test: Create `ParquetDocValuesIT` in `server/src/internalClusterTest/java/org/opensearch/index/codec/parquet/` extending `OpenSearchIntegTestCase`. Test with feature flag enabled: index mixed types, verify aggregations (terms, sum, date_histogram), sorting, `_source` reconstruction, `_update` API, force merge. Run with `-Dtests.iters=3`.
- [ ] Parquet export REST API: Create `RestParquetExportAction`, `ParquetExportAction`, `ParquetExportRequest`, `ParquetExportResponse`, `TransportParquetExportAction`. Endpoint: `POST /my-index/_parquet_export?path=<path>`. Reads doc_values via `ParquetDocValuesReader`, writes standalone `.parquet` files with valid footers. Write integration test. Run `./gradlew :server:internalClusterTest --tests "*ParquetExport*" -Dtests.iters=3`.
- [ ] Benchmark with opensearch-benchmark http_logs: Build with `./gradlew localDistro`. Run baseline (default settings) and parquet config. Compare: (1) correctness 100% pass (2) ingestion throughput 2x improvement (3) query performance within 10% (4) storage size 70% reduction.

## Completed
- [x] Feature flag and index setting: Added `opensearch.experimental.feature.parquet_doc_values.enabled` feature flag in `FeatureFlags.java`, `index.codec.doc_values.format` setting in `IndexSettings.java` with `isParquetDocValuesEnabled()` accessor, registered in `IndexScopedSettings.java`.
- [x] Parquet library dependency: Added `parquet-column`, `parquet-encoding`, `parquet-common` 1.15.1 to `server/build.gradle` via version catalog. Created LICENSE/NOTICE files under `server/licenses/`. Added thirdPartyAudit exclusions for parquet shaded classes. All precommit checks pass.
- [x] Lucene I/O adapters: Created `LuceneOutputFile` (adapts `IndexOutput` to Parquet `PositionOutputStream`) and `LuceneInputFile` (adapts `IndexInput` to Parquet `SeekableInputStream`) in `server/src/main/java/org/opensearch/index/codec/parquet/`. Tests in `LuceneIOAdapterTests` with 12 test methods × 10 iterations all passing.
